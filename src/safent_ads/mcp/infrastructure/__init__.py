"""Adaptadores de `mcp`: cuota en memoria (T044). Los adaptadores de los
puertos de lectura hacia otros contextos los cablea la integracion; esta
lane solo provee lo que necesita el propio dispatch."""
