"""Adaptadores SQL de las proyecciones de lectura de `rules.application
.read_models` (I-1, revision final T130): un modulo por proyeccion, mismo
nombre que su contraparte pura en `application/read_models/`."""

from __future__ import annotations
