"""Conectores propios contra APIs de terceros que no son plataformas de
anuncios (Meta/Google, que viven en `broker/`) ni proveedores cloud de
creatividad (`OPENAI_API_KEY`/`FAL_API_KEY`, en `BrokerSettings`).
"""
